# Copy this file to .env.local and fill in values. DO NOT commit secrets to git.

# Server-side secret (used only on the Render service)
GEMINI_API_KEY=
# Alternative name supported by server
API_KEY=

# Set to your frontend domain (or comma separated list)
# Example: https://example.com or https://example.com,https://www.example.com
FRONTEND_URL= https://pulseai.stackfellows.com 

# For building the frontend, set the base API URL the client will call
# The frontend reads VITE_API_URL at build time.
# Example used when building the Vite app before uploading to cPanel
VITE_API_URL=https://socialpulse-ttt.onrender.com
